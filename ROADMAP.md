# План развития team-docs

Живой документ: пункты дополняем, вычёркиваем по мере выполнения (`[x]`),
приоритеты пересматриваем. Даты — ориентиры, не обещания.

Статусы: `[ ]` — не начато · `[~]` — в работе · `[x]` — сделано.

---

## 1. Исправления (баги и несоответствия)

### 1.1 Поиск
- [x] **Починить мёртвый GIN-индекс.** Сделано (0004_search_russian.sql):
      генерируемая колонка `search_vector` + GIN по ней, запрос — по колонке.
- [x] **Русская морфология.** Сделано: конфиг `russian` в колонке, запросе и
      `ts_headline` (латиница идёт через english_stem внутри `russian`).
- [ ] (опц.) Опечатки/частичные совпадения: `pg_trgm` + similarity как fallback,
      когда FTS ничего не нашёл.

### 1.2 Дерево страниц
- [x] **Защита от циклов в `move`.** Сделано: общая `pages.Move`
      (internal/pages/move.go) с рекурсивным CTE; HTTP отвечает 400, MCP —
      ошибкой инструмента. Используется и хендлером, и MCP `move_page`.
- [x] **Переиндексация позиций соседей.** Сделано: «изъять + вставить» со
      сдвигом соседей и клампом позиции в одной транзакции; фронт теперь шлёт
      один PATCH за перетащенную страницу вместо N параллельных.

### 1.3 Файлы
- [x] **GC осиротевших файлов.** Сделано: janitor раз в сутки удаляет файлы
      без ссылок из контента страниц и ревизий (grace-период 24 ч).

---

## 2. Надёжность и качество

- [x] **Тесты конвертера `internal/blocknote`** — сделано: пустой документ,
      кламп заголовков, код-блоки, round-trip md → блоки → md.
- [x] **Тесты `ExtractText`** — сделано; тест фиксирует известное ограничение:
      текст в ячейках таблиц не попадает в content_text (walk не обходит rows).
- [x] **Тесты бэкапа**: round-trip (авторство/deleted_at/sequence), v1-дамп в
      дефолтный проект, битый дамп. В отдельной БД teamdocs_test_backup —
      Import перезаписывает всё, а пакеты go test бегут параллельно.
- [x] **Интеграционные тесты API**: move, поиск, корзина, HTTP CRUD +
      optimistic lock (409), janitor; юнит-тесты auth-гардов (HS256,
      401/403, publicRead, editorGroups). Скип без TEAMDOCS_TEST_DSN.
- [x] **CI**: `go test ./...` с postgres-сервисом добавлен.
- [x] **Retention ревизий**: janitor прореживает снапшоты старше 30 дней до
      одной (самой свежей) в день на страницу. `ListRevisions` по-прежнему
      отдаёт максимум 100 — с прореживанием этого хватает надолго.

---

## 3. Авторство и аудит

- [x] Миграция: users + `pages.created_by / updated_by`,
      `page_revisions.author_id` (0005); upsert по subject в auth-middleware
      (Registry с кэшем 5 мин; сброс кэша после импорта бэкапа).
- [x] Прокидывать пользователя из auth-контекста в `create/update`/ревизии
      (MCP-запись без identity даёт NULL).
- [x] UI: имя редактора в мета-строке страницы, автор в истории версий.
- [ ] Лента «последние изменения» (страница или блок на главной).

---

## 4. Масштабирование хранения

- [ ] **Стриминговый бэкап.** Сейчас экспорт собирает всю БД (включая файлы
      base64) в один JSON в памяти. Перейти на потоковый формат
      (NDJSON или zip с раздельными файлами), импорт — потоковое чтение.
- [ ] **Потоковая отдача файлов** вместо чтения BYTEA целиком в память;
      при росте — внешнее хранилище (S3-совместимое) за интерфейсом.

---

## 5. Дистрибуция

- [x] **Dockerfile** (multi-stage: web build → go build -tags prod →
      distroless/nonroot; смоук-тест пройден).
- [x] **docker-compose** для быстрого старта (app + postgres, volume).
- [x] **Релизы**: workflow на тег v* — кросс-сборка (linux/windows/macos,
      amd64+arm64) + SHA256SUMS (без GoReleaser — хватает голого go build).
- [ ] (опц.) Docker-образ в registry (ghcr) на тег; версия в бинаре
      (`-ldflags -X`).

---

## 6. Коллаборация

- [ ] **Presence (минимум):** индикатор «страницу редактирует X» —
      heartbeat/поллинг, без websocket.
- [ ] При 409 показывать диff/содержимое актуальной версии, а не только тост.
- [ ] **Реалтайм (максимум):** совместное редактирование через Yjs (BlockNote
      поддерживает из коробки) + websocket-слой на бэке. Большая задача —
      делать только после presence и по реальной потребности.
- [ ] **Уведомления и подписки**: «следить за страницей/поддеревом»,
      @-упоминания пользователей (сейчас есть только @-страницы), центр
      уведомлений в UI; (опц.) почтовые дайджесты. Зависит от `users`
      (раздел 8).

---

## 7. Продуктовые фичи

- [ ] **draw.io-блок** — задел готов (таблица `diagrams`, sqlc-запросы),
      осталось: HTTP-модуль, UI-блок с embed-редактором, превью.
- [ ] Экспорт поддерева целиком (zip из `.md` с вложенностью и файлами).
- [ ] Шаблоны страниц (создать из шаблона; шаблоны — обычные страницы в
      служебном разделе).
- [ ] Метки/теги страниц + фильтр в поиске.
- [ ] Избранное / закреплённые страницы в сайдбаре.
- [ ] Комментарии: к странице целиком **и инлайн к выделенному тексту**
      (BlockNote поддерживает); треды, разрешение (resolve).
- [x] **Корзина.** Сделано (0006): мягкое удаление поддерева, `GET /api/trash`,
      restore (родитель удалён → в корень), purge, автоочистка 30 дней,
      UI в сайдбаре; MCP `delete_page` тоже мягкий.
- [ ] **Публикация страницы по ссылке** (public sharing): токен-ссылка для
      внешних читателей без аккаунта, отзыв ссылки, только чтение;
      (опц.) публикация поддерева.
- [ ] **Diff версий** — сравнение двух ревизий («что изменилось»), а не только
      просмотр и откат целиком.
- [ ] **Embed-блоки** — iframe-блок с allowlist сервисов (YouTube, Miro,
      Figma, Loom…).
- [ ] **Формулы (LaTeX)** — inline и блочная математика (KaTeX).
- [ ] **Excalidraw-блок** — скетчи от руки (дополнение к Mermaid/draw.io).
- [ ] Архивирование страниц (промежуточное состояние между «живая» и
      «удалена»; скрыта из дерева и поиска, доступна по ссылке).
- [ ] Задачи с исполнителями: агрегация чек-листов «мои задачи по всем
      страницам» (зависит от таблицы `users`, раздел 8).
- [ ] **Импорт из Confluence** — только через REST API (решение: XML/HTML-экспорты
      не поддерживаем). Подзадачи:
  - [ ] API-клиент (Cloud и Server/DC: разные пути и авторизация, токен):
        обход спейса, получение страниц в storage format, `ancestors`
        (иерархия), список и скачивание вложений.
  - [ ] Конвертер storage-XHTML → блоки BlockNote (`internal/confluence`,
        отдельный от markdown-конвертера — иначе теряются панели/toggle/статусы):
        заголовки, списки, таблицы, цитаты, код (`code`), панели
        (`info/tip/warning/note` → выноски), `status` → статус-бейджи,
        `expand` → toggle, task lists → чек-листы.
  - [ ] Вложения: скачивание → запись в `files` → перезапись ссылок
        (`ri:attachment`) в контенте; `ri:page` → @-упоминания страниц.
  - [ ] Fallback для неизвестных макросов: ничего не терять молча — выноска
        «не удалось импортировать: <макрос>» с исходником внутри toggle.
  - [ ] CLI-подкоманда в том же бинаре:
        `team-docs import-confluence --base-url --token --space KEY [--parent-id N]`.
  - [ ] Возобновляемость и отчёт: импорт по страницам (не одной транзакцией),
        итог «N страниц, M макросов не распознано (список)», повторный запуск
        не дублирует уже импортированное.
  - [ ] Пометка происхождения: «импортировано из Confluence, автор X»
        (зависит от раздела 3 «Авторство»).
  - [ ] (опц.) draw.io-вложения (.drawio XML) → таблица `diagrams` — связка
        с задачей draw.io-блока.

---

## 8. Авторизация: социальные провайдеры

Сейчас приложение только валидирует JWT от внешнего IAM-прокси. Цель — вход
через популярных провайдеров (Google, Apple, Yandex, VK и др.) без обязательного
внешнего IAM.

- [x] **Архитектурное решение**: встроенный OAuth2 (2026-07-19) — в духе
      «одного бинаря», без обязательного Keycloak; IAM-режим (JWT из
      заголовка) сохранён, middleware пробует cookie-сессию → заголовок.
- [x] Встроенный OAuth2-флоу: `/auth/login/:key`, callback (state-cookie),
      `/auth/logout`, cookie-сессия HS256 (`auth.sessionSecret`, TTL 30 дней;
      без refresh — сессия долгоживущая).
- [x] Провайдеры: Google (OIDC userinfo), Yandex, VK (user_id/email из
      token-ответа), Apple (ES256 client_secret из .p8, form_post-callback).
      Настройка: `auth.providers.*` + `auth.publicUrl`.
- [x] Роли в БД: reader / editor / admin (`users.role`, 0008), дефолт
      `auth.defaultRole`; бутстрап админов `auth.adminEmails`; `RequireAdmin`;
      маппинг групп из JWT сохранён для IAM-режима (приоритетнее роли).
- [x] Экран входа: кнопки настроенных провайдеров (`GET /auth/providers`),
      `publicRead` сохраняется; выход — в футере сайдбара.
- [ ] Привязка нескольких провайдеров к одному пользователю (по email или
      явная привязка в профиле).
- [x] **Группы пользователей** (0010): локальные группы + состав в админке;
      роль группе в проекте (`PUT /api/projects/:id/groups/:groupId`);
      приоритет: личное членство > групповое > видимость. UI назначения
      группы проекту — пока только API.

## 9. Страница администрирования

Все настройки, которые сейчас живут только в `appsettings.yaml`/env, должны
управляться администратором через UI без рестарта.

- [x] **Хранение настроек в БД** (`internal/settings`, JSONB) с приоритетом
      **env > yaml > БД > дефолт**; применяется сразу (брендинг и
      maxUploadBytes читаются динамически). Пока управляются: брендинг
      (название/пространство/монограмма/тема) и лимит загрузки; auth-секция —
      только через конфиг (секреты в UI не выносим).
- [x] **Блокировка конфигом**: провенанс на ключ без доработки sconf —
      env определяется через LookupEnv, yaml — прямым парсингом
      appsettings.yaml; API отдаёт `value + source + editable`, UI показывает
      замочек «управляется конфигурацией». Аудит: `updated_by/updated_at`
      в таблице settings.
- [x] **Роль администратора** + гард `RequireAdmin`; первый админ — через
      `auth.adminEmails` (бутстрап при входе).
- [x] API и UI пользователей/ролей: `GET /api/admin/users`,
      `PUT /api/admin/users/:id/role`; страница `/admin` с таблицей
      пользователей и селектором ролей (вход из сайдбара для админов).
- [x] API настроек: `GET/PUT /api/admin/settings` + секция «Настройки»
      на `/admin` (инпуты, замочки для env/yaml-ключей).
- [ ] UI-страница `/admin` (видна только админам), секции:
  - [ ] Брендинг: название, монограмма, тема по умолчанию (сейчас —
        `branding` в yaml).
  - [ ] Авторизация: вкл/выкл, режим (IAM JWT / встроенные провайдеры),
        JWKS/issuer/секрет, `publicRead`, `editorGroups`, настройка
        провайдеров из раздела 8.
  - [ ] Пользователи и роли: список, назначение читатель/редактор/админ.
  - [ ] Лимиты: `maxUploadBytes`, retention ревизий (связка с разделом 2).
  - [ ] Данные: перенести сюда экспорт/импорт БД (сейчас в меню оформления)
        и запуск GC файлов (раздел 1.3).
  - [ ] Импорт: запуск импорта из Confluence с прогрессом (раздел 7) —
        альтернатива CLI.
- [ ] Кнопка «проверить подключение» для JWKS/провайдеров (валидация настроек
      до сохранения).

---

## 10. Проекты (пространства) и их ролевая модель

Сейчас все страницы живут в одном общем дереве, а права глобальные
(читатель/редактор на всё приложение). Цель — изоляция по проектам в духе
спейсов Confluence: у каждого проекта своё дерево страниц и свои участники
с ролями.

- [x] **Модель данных**: `projects` + `visibility`, `pages.project_id NOT NULL`
      (0007/0009), существующие страницы — в дефолтном 'main'.
- [x] **Ролевая модель на проект**: `project_members` (reader/editor/admin);
      членство перекрывает глобальную роль (и поднимает, и ограничивает);
      глобальный админ — админ везде.
- [x] **Видимость проекта**: public / internal / private (RoleFor;
      аноним читает только public при publicRead).
- [x] **Гарды на бэке**: guardRead/guardWrite во всех хендлерах pages
      (404 для невидимых, 403 читателям), дерево по проекту, поиск и корзина
      фильтруются по доступным. Файлы: проект определяется по странице,
      использующей UUID файла; не привязанные видят только редакторы.
- [x] **UI**: переключатель проектов в сайдбаре (localStorage), создание
      страниц в текущем проекте; управление проектами/видимостью/участниками —
      секция «Проекты» в /admin.
- [~] **Затронутые подсистемы**:
  - [x] поиск: фильтр по доступным проектам (+ найден и исправлен баг:
        поиск не фильтровал корзину);
  - [x] MCP: параметр project у list_pages/search_pages/create_page
        (по умолчанию 'main'; проектные роли к MCP не применяются —
        доверенная интеграция);
  - [x] бэкап: projects/visibility/members в дампе (экспорт одного проекта —
        отдельная задача раздела 4);
  - [ ] импорт из Confluence: спейс → проект (раздел 7);
  - [x] move: перенос между проектами запрещён (400).
- [ ] (опц.) Ограничения на уровне страницы (page restrictions, как в
      Confluence) — поверх проектных прав; только при реальной потребности.
- [ ] Зависимости: таблица `users` и роли (раздел 8), назначение ролей через
      админку (раздел 9). Делать после них — иначе некому назначать права.

---

## 11. Главная страница (в духе DocMost)

Сейчас `HomeScreen` минималистичный. Цель — главная как хаб: поиск в центре,
обзор проектов и активности.

- [ ] **Поиск по центру** — крупное поле, открывающее командную палитру
      (`⌘K` уже есть — переиспользовать), недавние запросы.
- [ ] **Карточки проектов** (после раздела 10): доступные пользователю
      проекты с иконкой, описанием и счётчиком страниц.
- [ ] **Популярные проекты/страницы** — требует счётчика просмотров:
      таблица `page_views` (или агрегат по дням), запись при `GET /pages/:id`,
      топ за период. До появления проектов — просто популярные страницы.
- [ ] **Недавно обновлённые** — лента последних изменений по доступным
      страницам (связка с разделом 3 «Авторство»: показывать кто правил).
- [ ] **Продолжить чтение** — недавно открытые текущим пользователем
      (сейчас recents уже есть в localStorage — можно начать с него,
      позже перенести на сервер к профилю).
- [ ] (опц.) Избранное на главной (связка с «Избранное» из раздела 7).
- [ ] (опц.) Приветственный блок с быстрыми действиями: создать страницу,
      импортировать Markdown, открыть тур.

Поэтапность: поиск + недавние можно сделать сразу (без зависимостей);
популярность требует счётчика просмотров; карточки проектов — после раздела 10.

---

## 12. Кэширование (in-memory / Redis)

Кэш за единым интерфейсом; бэкенд выбирается конфигом. По умолчанию —
встроенный in-memory (ноль внешних зависимостей, в духе «одного бинаря»).

- [ ] **Интерфейс кэша** (`internal/cache`): Get/Set/Delete + инвалидация,
      TTL; реализация подключается по конфигу `cache.backend: memory | redis`.
- [ ] **In-memory (дефолт)**: LRU с лимитом по памяти/количеству + TTL.
- [ ] **Что кэшируем (первый этап — страницы)**: `GET /pages/:id` (контент),
      дерево страниц, брендинг. Позже — результаты поиска (короткий TTL).
- [ ] **Инвалидация** — самое важное: сброс при `PUT/PATCH/DELETE` страницы,
      move (дерево), импорте бэкапа/Confluence (полный сброс). Ключ версии
      страницы уже есть (`version`) — можно использовать для валидации.
- [ ] **Redis-бэкенд**, режимы подключения:
  - [ ] standalone;
  - [ ] **Redis Cluster**;
  - [ ] **Sentinel** (имя мастера + список сентинелов);
  - [ ] **TLS и без TLS** для всех трёх режимов (+ опц. mTLS: клиентский
        сертификат, свой CA, skip-verify для dev);
  - [ ] auth: пароль/ACL-пользователь.
- [ ] **Многоэкземплярный режим**: при in-memory и нескольких репликах
      приложения инвалидация не распространяется — задокументировать
      ограничение (in-memory = один экземпляр); для реплик — Redis
      (+ pub/sub-инвалидация, если понадобится гибрид).
- [ ] Конфиг в `appsettings.yaml`/env (`TEAMDOCS_CACHE__…`), позже — секция
      в админке (раздел 9) + кнопка «проверить подключение» и «сбросить кэш».
- [ ] Метрики: hit/miss в логи или `/api/health` (для начала достаточно).

---

## Порядок работ (рекомендация)

Этапы идут по зависимостям: сначала решения по инструментам и фундамент схемы,
потом защита данных, права и структура, затем удобства. Внутри этапа порядок
свободный.

Принцип: заранее делаем только **структурные** изменения схемы — те, что
трогают `pages` и требуют бэкфилла (project_id, авторство, deleted_at,
tsvector). Аддитивные сущности (комментарии, уведомления, page_views,
подписки) — новые таблицы, их проектируем в момент реализации фичи, не впрок.

### Этап 0 — фундамент схемы ✅ (выполнен 2026-07-19)
1. [x] Слой данных: остаёмся на sqlc (спайк sorm отменён).
2. [x] Волна миграций 0004–0007: tsvector+GIN, users+авторство, deleted_at,
   projects + дефолтный 'main' + `pages.project_id NOT NULL`, settings.
   Бэкап поднят до формата v2 (users/projects/авторство/deleted_at),
   v1-дампы принимаются (страницы → дефолтный проект).
3. [x] Поверх волны включены: запись авторства, корзина, русский поиск.

### Этап 1 — гигиена и защита данных
4. **1.2 Дерево** — защита от циклов в `move` (дыра доступна через MCP) +
   переиндексация позиций.
5. **2 Тесты ядра + CI-шаг** — конвертер blocknote, ExtractText, бэкап;
   страховка перед всем остальным.

### Этап 2 — эксплуатационная база
6. **Retention ревизий** и **1.3 GC файлов** (2, 1.3) — пока базы маленькие.
7. **5 Дистрибуция** — Dockerfile + compose + релизы; делает проект
   отдаваемым наружу и упрощает тестовые стенды для этапов ниже.

### Этап 3 — доступ и структура (строгая цепочка 8 → 9 → 10)
8. **8 Авторизация**: сначала архитектурное решение (встроенный OAuth vs
   Keycloak-брокеринг), затем провайдеры, роли, группы.
9. **9 Админка**: настройки в БД, роль админа, страница `/admin`.
10. **10 Проекты** с ролевой моделью — самый крупный структурный сдвиг;
    делать до накопления большого объёма страниц.

### Этап 4 — ежедневный опыт пользователя
11. **11 Главная-хаб** (поиск/недавние — сразу; карточки проектов — после
    этапа 3; популярность — добавив счётчик просмотров).
12. **Теги, избранное, шаблоны** (7) — навигационные удобства.
13. **Presence + UX конфликта 409** (6), **diff версий** (7).
14. **Публикация по ссылке** (7) и **уведомления/подписки** (6).

### Этап 5 — редактор и контент-обмен
15. **Embed-блоки, LaTeX, Excalidraw** (7) — независимые блоки, можно
    брать по одному.
16. **draw.io-блок** (7) — задел в схеме уже есть.
17. **Импорт из Confluence** (7) и **экспорт поддерева** — после проектов
    (спейс → проект) и авторства (пометка происхождения).

### Этап 6 — масштаб (по реальной потребности, не впрок)
18. **12 Кэш** — когда появится измеримая нагрузка; сначала in-memory.
19. **4 Масштабирование хранения** — стриминговый бэкап и потоковые файлы,
    когда база вырастет.
20. **Yjs-реалтайм** (6), **архивирование, задачи, page restrictions** (7, 10)
    — по запросам команды.

---

## Журнал решений

| Дата | Решение |
|---|---|
| 2026-07-19 | Слой данных остаётся на sqlc; спайк и переход на sorm отменены. Рекурсивные CTE и прочий SQL, не проходящий анализатор sqlc, живут сырым pgx (прецеденты: backup, pages/move.go). |
| 2026-07-19 | Раздел 9 (первая часть): /api/admin/users + смена ролей (со сбросом кэша Registry) и страница /admin. Настройки в БД с провенансом — следующий кусок §9. |
| 2026-07-20 | Хвосты этапа 3: MCP-параметр project, гард /api/files (проект по использующей странице), canEdit страницы в UI (кнопки правки скрыты у проектных читателей), локальные группы (0010) с ролями в проектах и секцией в админке. Найден и исправлен баг: Migrate без блокировки гонялся при параллельном старте — добавлен pg_advisory_lock (воспроизведено параллельными тест-пакетами). Осталось из этапа 3: привязка нескольких провайдеров, секция «Данные» в админке, UI назначения группы проекту. |
| 2026-07-20 | Раздел 10 (ядро) реализован: visibility + project_members (0009), RoleFor с семантикой «членство перекрывает глобальную роль», гарды во всех pages-хендлерах, переключатель проектов в сайдбаре, управление в /admin. MCP закреплён за 'main'; файлы пока без проектного гарда; UI не скрывает кнопки правки для проектных читателей (бэкенд отсекает). |
| 2026-07-19 | Раздел 9: настройки в БД реализованы (internal/settings). Провенанс без доработки sconf: env — LookupEnv, yaml — прямой парсинг appsettings.yaml. Управляемые ключи: брендинг + maxUploadBytes; auth-секция намеренно только в конфиге (секреты не выносим в UI). Осталось из этапа 3: локальные группы, привязка провайдеров (§8), проекты (§10). |
| 2026-07-19 | Раздел 8 (ядро): решение — встроенный OAuth без обязательного Keycloak. Реализованы cookie-сессии, провайдеры Google/Yandex/VK/Apple, роли reader/editor/admin (0008) с бутстрапом через adminEmails, RequireAdmin, экран входа. Флоу покрыт интеграционным тестом с фейковым IdP. Осталось из §8: привязка нескольких провайдеров, локальные группы. |
| 2026-07-19 | Этапы 1–2 закрыты: тесты бэкапа/auth/HTTP-CRUD (все прогнаны на живом Postgres), janitor (корзина + прореживание ревизий + GC файлов), Dockerfile (смоук пройден), docker-compose, release-workflow на тег. package-lock.json закоммичен (не был в git — npm ci в CI/Docker падал); полный лок генерился в linux-контейнере из-за бага npm на Windows с optional deps. |
| 2026-07-19 | Этап 0 выполнен (миграции 0004–0007): users+авторство, корзина, скелет проектов (все страницы в дефолтном 'main'), settings. Бэкап — формат v2, v1 принимается. Импорт бэкапа сбрасывает кэш Registry (id пользователей пересоздаются). Тесты: конвертер, ExtractText, интеграционные move/поиск/корзина; go test в CI с postgres. Не прогнано вживую локально (Docker/Postgres выключены) — проверит CI. |
| 2026-07-19 | Реализованы 1.1 и 1.2. Семантика PATCH /pages/:id/move изменилась: position — индекс вставки среди детей без самой страницы, сервер клампит значение, переиндексирует соседей атомарно и отвечает 400 при переносе в собственное поддерево; фронт шлёт один PATCH вместо переиндексации всех сиблингов. Рекурсивный CTE не проходит анализатор sqlc — живёт сырым SQL в internal/pages/move.go. |
| 2026-07-19 | План создан по итогам ревью кода (поиск, move, тесты, авторство). |
| 2026-07-19 | Импорт из Confluence — только через REST API; XML/HTML-экспорты не поддерживаем. Конвертер — прямой storage-XHTML → блоки, не через Markdown. |
| 2026-07-19 | Добавлены разделы 8 (вход через Google/Apple/Yandex/VK) и 9 (админ-страница настроек). Открытый вопрос раздела 8: встроенный OAuth vs брокеринг через Keycloak — решить до реализации. |
| 2026-07-19 | Добавлен раздел 10: проекты (пространства) с ролевой моделью на проект (читатель/редактор/админ проекта, видимость публичный/внутренний/приватный). Делается после разделов 8–9. |
| 2026-07-19 | Добавлен раздел 11: главная-хаб в духе DocMost (поиск по центру, карточки проектов, популярные/недавние, «продолжить чтение»). Первый этап без зависимостей; популярность требует счётчика просмотров, карточки проектов — раздела 10. |
| 2026-07-19 | Добавлен раздел 12: кэш за интерфейсом, дефолт — in-memory (страницы), опционально Redis (standalone / Cluster / Sentinel, с TLS и без). In-memory = один экземпляр приложения; для реплик — Redis. |
| 2026-07-19 | По итогам сравнения с DocMost/Confluence добавлены: корзина (мягкое удаление), публикация по ссылке, diff версий, embed-блоки, LaTeX, Excalidraw, архивирование, задачи с исполнителями (раздел 7); группы пользователей (8); уведомления/подписки (6); page restrictions опционально (10). Открытый вопрос: нужна ли локализация UI на другие языки (сейчас только русский). |
| 2026-07-19 | Раздел 9: настройки, явно заданные в yaml/env, блокируются в админке (read-only с бейджем «управляется конфигурацией»). Приоритет пересмотрен: конфиг (env, yaml) > БД > дефолты. Нужна доработка sconf — провенанс ключей. |
| 2026-07-19 | Добавлен этап 0: сначала спайк sqlc vs sorm (на этом слое пишутся все дальнейшие миграции), затем одна волна структурных миграций (users, авторство, deleted_at, projects+project_id, settings, tsvector) с бэкфиллом на малой базе. Аддитивные сущности (комментарии, уведомления, page_views) впрок не проектируем. |
| 2026-07-19 | Порядок работ переписан в 6 этапов: 1) гигиена/защита данных (корзина, поиск, циклы move, тесты) → 2) users+авторство, retention, дистрибуция → 3) авторизация → админка → проекты → 4) главная, теги, presence, шаринг → 5) блоки редактора и Confluence-импорт → 6) масштаб (кэш, стриминг, Yjs) по потребности. |
